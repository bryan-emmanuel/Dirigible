apply plugin: 'java'
apply plugin: 'war'

dependencies {
    providedCompile 'javax.servlet:javax.servlet-api:3.0.1'
    compile fileTree(dir: 'libs', include: ['*.jar'])
}

sourceCompatibility = "1.7"
targetCompatibility = "1.7"

war {
    archiveName 'dirigible.war'
}
